# 文档解析与分块难题方案（4.5_15：30）

> 基于 RAGFlow v0.24.0 源码分析，聚焦技术手册等行业文档的处理策略

---

## 一、核心挑战

技术手册类文档有三个特殊性，决定了不能用通用的固定 token 数盲切：

1. **术语密度高**：缩写、专有名词、型号编号，脱离上下文就失去意义
2. **层级结构深**：章 → 节 → 条款 → 子条款，切片后丢失归属关系
3. **跨段落依赖**："如第 3.2 节所述"、"参见上表"，孤立切片后引用断裂

---

## 二、策略一：标题层级感知切片

### 问题

固定 token 数切片会把同一节的内容切断，也会把不同节的内容混在一起。更严重的是，切出来的 chunk 丢失了它的"归属"——检索命中了一段参数描述，但不知道它属于哪个章节。

### 目标

每个 chunk 应携带其完整的标题路径，例如：

```
第3章 液压系统 > 3.1 主泵规格
额定压力：350bar，流量：120L/min
```

而不是孤立的：

```
额定压力：350bar，流量：120L/min
```

### RAGFlow 实现

**代码位置**：`rag/app/manual.py` → `chunk()` 函数，L196–230

RAGFlow 对 PDF 和 Docx 采用不同路径：

**PDF：PDF Outline 优先，正则兜底**

```python
# rag/app/manual.py L196-230
outlines = extract_pdf_outlines(binary if binary is not None else filename)

if len(sections) > 0 and len(outlines) / len(sections) > 0.03:
    # 有 PDF 书签/目录：用 outline 匹配每个文本块的层级
    for txt, _, _ in sections:
        for t, lvl, _ in outlines:
            # 用双字符 bigram 相似度匹配标题（相似度 > 0.8 认为命中）
            tks  = set([t[i] + t[i+1] for i in range(len(t) - 1)])
            tks_ = set([txt[i] + txt[i+1] for i in range(min(len(t), len(txt) - 1))])
            if len(set(tks & tks_)) / max([len(tks), len(tks_), 1]) > 0.8:
                levels.append(lvl)
                break
        else:
            levels.append(max_lvl + 1)  # 非标题内容
else:
    # 无目录：用 bullets_category + title_frequency 自动识别标题模式
    bull = bullets_category([txt for txt, _, _ in sections])
    most_level, levels = title_frequency(bull, [(txt, lvl) for txt, lvl, _ in sections])
```

`title_frequency()` 在 `rag/nlp/__init__.py` L901，通过统计各级编号模式（`第X章`、`X.X`、`(X)` 等）的出现频率，自动推断哪一级是"最常见的正文级别"，从而确定切片边界。

**Docx：标题栈维护祖先路径**

`rag/app/manual.py` 的 `Docx.__call__()` L74，用一个**标题栈**维护当前位置的完整层级路径：

```python
question_stack, level_stack = [], []

for p in self.doc.paragraphs:
    question_level, p_text = docx_question_level(p)  # 识别标题级别

    if not question_level or question_level > 6:  # 正文内容
        last_answer = f"{last_answer}\n{p_text}"
    else:  # 是标题
        if last_answer:
            # chunk = 完整祖先标题路径 + 当前节正文
            sum_question = "\n".join(question_stack)
            ti_list.append((f"{sum_question}\n{last_answer}", last_image))

        # 维护标题栈：弹出同级或更深的标题，保留祖先
        while question_stack and question_level <= level_stack[-1]:
            question_stack.pop()
            level_stack.pop()
        question_stack.append(p_text)
        level_stack.append(question_level)
```

每个 chunk 的内容是 `祖先标题路径 + 当前节正文`，检索时即使只命中正文，也能知道它属于哪个章节。

---

## 三、策略二：Section ID 合并（同节内容不拆散）

### 问题

识别出层级后，同一节内的多个段落可能因为 token 数限制被强行切断，导致语义不完整。

### RAGFlow 实现

**代码位置**：`rag/app/manual.py` L232–270

```python
# 为每个文本块分配 section id
sec_ids = []
sid = 0
for i, lvl in enumerate(levels):
    if lvl <= most_level and i > 0 and lvl != levels[i - 1]:
        sid += 1  # 遇到新标题才递增，同节内容共享同一 sid
    sec_ids.append(sid)

# 合并时：同节内容优先不切断
chunks = []
last_sid = -2
tk_cnt = 0
for txt, sec_id, poss in sorted(sections, ...):
    if tk_cnt < 32 or (tk_cnt < 1024 and (sec_id == last_sid or sec_id == -1)):
        # 同一 section 且未超 1024 token：强制合并
        chunks[-1] += "\n" + txt + poss
        tk_cnt += num_tokens_from_string(txt)
        continue
    chunks.append(txt + poss)
    tk_cnt = num_tokens_from_string(txt)
```

两个关键阈值：
- `tk_cnt < 32`：太短的块无条件合并到上一块，避免碎片
- `tk_cnt < 1024 and sec_id == last_sid`：同节内容最多合并到 1024 token，不跨节

---

## 四、策略三：Overlap 滑动窗口（通用模式的上下文保留）

### 问题

对于没有明显标题结构的文档，相邻 chunk 之间的边界处语义会断裂。

### RAGFlow 实现

**代码位置**：`rag/nlp/__init__.py` → `naive_merge()` L1070

```python
def add_chunk(t, pos):
    if cks[-1] == "" or tk_nums[-1] > chunk_token_num * (100 - overlapped_percent) / 100.:
        # 新建 chunk 时，把上一个 chunk 末尾的 overlapped_percent% 内容带过来
        overlapped = RAGFlowPdfParser.remove_tag(cks[-1])
        t = overlapped[int(len(overlapped) * (100 - overlapped_percent) / 100.):] + t
        cks.append(t)
    else:
        cks[-1] += t  # 未超限：直接追加到当前 chunk
```

通过 `overlapped_percent` 参数控制重叠比例，确保边界处的语义连续性。

---

## 五、策略四：表格与图片的上下文附加

### 问题

技术手册里表格和图片往往是核心内容（参数表、结构图），但孤立的表格没有语义——"下表列出了各型号的额定参数"这句话和表格本身被切开后，表格就失去了解释。

### RAGFlow 实现

**代码位置**：`rag/nlp/__init__.py` → `_add_context()` L1362，`attach_media_context()` L409

```python
# 向前找 context_size 个 token 的文字内容附加到表格/图片 chunk
while prev >= 0 and remain_above > 0:
    if cks[prev]["ck_type"] == "text":
        piece = take_sentences_from_end(cks[prev]["text"], remain_above)
        parts_above.insert(0, piece)
        remain_above -= tk
    prev -= 1

cks[idx]["context_above"] = "".join(parts_above)
cks[idx]["context_below"] = "".join(parts_below)
```

通过 `parser_config` 中的两个参数控制：
- `table_context_size`：表格前后附加的 token 数
- `image_context_size`：图片前后附加的 token 数

---

## 六、策略五：目录页过滤

### 问题

技术手册的目录页如果被索引进去，会严重干扰检索——目录条目和正文内容高度相似，但目录本身没有实质内容。

### RAGFlow 实现

**代码位置**：`rag/nlp/__init__.py` → `remove_contents_table()` L847

```python
# 识别目录页标志词
if not re.match(
    r"(contents|目录|目次|table of contents|致谢|acknowledge)$",
    re.sub(r"( | |\u3000)+", "", get(i).split("@@")[0], flags=re.IGNORECASE)
):
    i += 1
    continue
# 找到目录页后，把后续的目录条目也一并删除
sections.pop(i)
prefix = get(i)[:3]  # 取目录第一条的前缀，用于识别后续条目
while ...:
    sections.pop(i)
```

---

## 七、策略六：层级树合并（高级模式）

对于结构复杂的文档，RAGFlow 还提供了两种更精细的合并策略：

**代码位置**：`rag/nlp/__init__.py`

| 函数 | 行号 | 说明 |
|------|------|------|
| `tree_merge()` | L931 | 构建标题树，按深度参数决定切片粒度 |
| `hierarchical_merge()` | L980 | 层级化合并，把子节内容归并到父节 chunk |

`tree_merge()` 的核心思路：把文档解析成一棵标题树，`depth` 参数控制在哪一层切片。`depth=1` 按章切，`depth=2` 按节切，以此类推。

```python
# rag/nlp/__init__.py L931
def tree_merge(bull, sections, depth):
    # 识别每个段落的标题级别
    lines = [(get_level(bull, section), text) for section in sections]
    # 按 depth 参数确定切片层级
    target_level = sorted_levels[depth - 1]
    # 构建树并按目标层级输出 chunk
    root = Node(level=0, depth=target_level, texts=[])
    root.build_tree(lines)
    return [element for element in root.get_tree() if element]
```

---

## 八、代码位置速查表

| 策略 | 文件 | 函数/位置 |
|------|------|---------|
| 技术手册主入口 | `rag/app/manual.py` | `chunk()` L137 |
| PDF 目录层级提取与匹配 | `rag/app/manual.py` | `chunk()` L196–230 |
| Section ID 分配与合并 | `rag/app/manual.py` | `chunk()` L232–270 |
| Docx 标题栈 | `rag/app/manual.py` | `Docx.__call__()` L74 |
| 标题模式自动识别 | `rag/nlp/__init__.py` | `title_frequency()` L901 |
| 层级树合并 | `rag/nlp/__init__.py` | `tree_merge()` L931 |
| 层级化合并 | `rag/nlp/__init__.py` | `hierarchical_merge()` L980 |
| Overlap 滑动窗口 | `rag/nlp/__init__.py` | `naive_merge()` L1070 |
| 表格/图片上下文附加 | `rag/nlp/__init__.py` | `_add_context()` L1362 |
| 目录页过滤 | `rag/nlp/__init__.py` | `remove_contents_table()` L847 |
| Docx 标题级别识别 | `rag/nlp/__init__.py` | `docx_question_level()` L1201 |

---

## 九、当前方案的局限与补充建议

RAGFlow 的现有方案已经覆盖了大多数场景，但对于**行业术语极度密集**的文档，还有以下可以补充的方向：

**1. 术语词典注入**

在分词阶段把行业术语作为不可分割的原子单元，避免 `液压泵` 被切成 `液压` + `泵`。RAGFlow 的 `rag_tokenizer` 支持自定义词典，但需要手动维护领域词表。

**2. 跨 chunk 引用解析**

遇到"如第 3.2 节所述"这类引用，在索引时把被引用节的摘要也附加进来。当前项目没有做这一步，需要在 `tokenize_chunks()` 阶段增加引用解析逻辑。

**3. LLM 辅助标题识别**

对于没有标准编号格式的手册（比如用粗体而非编号区分标题），纯正则识别会失效。可以用轻量 LLM 判断段落是否为标题。RAGFlow 的 MinerU / Docling 后端在一定程度上解决了这个问题（通过视觉模型识别版面），可通过 `USE_DOCLING=true` 或 `MINERU_BACKEND` 环境变量启用。
