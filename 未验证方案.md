下面给你一份**可直接交给 AI 编程工具落地的系统设计与关键模块设计文档（工程+论文双用版）**。我按“能实现”为第一目标写，包含接口、数据结构、流程与伪代码，方便你在现有 RAG 上逐步替换。

---

# 🏥 医疗技术文档智能问答系统（RAG 2.0）设计文档

## 1. 项目目标

构建面向**医疗内部技术文档**的高可靠问答系统，满足：

* **高准确性**：严控幻觉（≤3%）
* **强可溯源**：答案必须带引用（句级/段级）
* **高召回**：≥90%
* **低延迟**：≤3s
* **长对话稳定**：20+轮不丢关键约束

---

## 2. 总体架构

```text
User Query
   ↓
[1] Query Rewrite（多轮理解）
   ↓
[2] Hybrid Retrieval（向量 + BM25 + Metadata）
   ↓
[3] Rerank（Cross-Encoder）
   ↓
[4] Context Builder（结构化上下文）
   ↓
[5] Grounded LLM Generation（受控生成）
   ↓
[6] Citation Alignment（引用对齐）
   ↓
[7] Answer Verification（二次校验）
   ↓
Final Answer（带来源）
```

---

## 3. 系统模块划分

```text
rag_system/
├── ingestion/          # 文档处理
├── retriever/          # 检索模块
├── reranker/           # 重排序
├── context_builder/    # 上下文构建
├── generator/          # LLM生成
├── verifier/           # 答案校验
├── memory/             # 对话记忆（你已有）
├── api/                # 对外接口
```

---

# 🔧 4. 核心模块设计

---

# 4.1 文档处理模块（ingestion）

## 🎯 目标

解决：

* 表格丢失 ❌
* 切片断裂 ❌
* 专业术语丢上下文 ❌

---

## 4.1.1 数据结构

```python
class DocumentChunk:
    def __init__(self, content, doc_id, section, chunk_id, metadata):
        self.content = content
        self.doc_id = doc_id
        self.section = section
        self.chunk_id = chunk_id
        self.metadata = metadata  # type, keywords, etc.
```

---

## 4.1.2 结构感知切片（核心）

```python
def split_document(doc):
    chunks = []

    for section in doc.sections:
        for paragraph in section.paragraphs:
            chunks.append(create_chunk(paragraph))

        for table in section.tables:
            text = table_to_text(table)
            chunks.append(create_chunk(text, type="table"))

    return chunks
```

---

## 4.1.3 表格处理（必须做）

```python
def table_to_text(table):
    rows = []
    for r in table:
        rows.append(" | ".join(r))
    return "\n".join(rows)
```

👉 推荐升级（更强）：

```python
# JSON结构（更适合医疗）
{
  "drug": "A",
  "dose": "10mg",
  "side_effect": "..."
}
```

---

## 4.1.4 向量入库

```python
vector_store.upsert(
    id=chunk_id,
    vector=embedding,
    payload={
        "doc_id": doc_id,
        "section": section,
        "text": content,
        "type": "table/text"
    }
)
```

---

# 🔍 4.2 检索模块（retriever）

---

## 4.2.1 Hybrid Retrieval

```python
def hybrid_retrieve(query):
    vec_results = vector_search(query, top_k=10)
    bm25_results = bm25_search(query, top_k=10)

    merged = merge_results(vec_results, bm25_results)
    return deduplicate(merged)
```

---

## 4.2.2 Metadata过滤

```python
def filter_by_metadata(results, filters):
    return [r for r in results if r.metadata["type"] in filters]
```

---

## 🔥 提升点

* 召回率：65% → 85~90%

---

# 🧠 4.3 重排序模块（reranker）

---

## 4.3.1 接口设计

```python
def rerank(query, candidates):
    scored = []
    for c in candidates:
        score = cross_encoder.predict(query, c.text)
        scored.append((c, score))

    return sorted(scored, key=lambda x: x[1], reverse=True)[:5]
```

---

## 🎯 作用

* 去噪
* 提升精确度
* 降低幻觉

---

# 🧱 4.4 上下文构建模块（Context Builder）

---

## 4.4.1 构建格式（强约束）

```python
def build_context(query, chunks):
    context = "【参考资料】\n"

    for i, c in enumerate(chunks):
        context += f"[{i+1}] ({c.doc_id}-{c.section})\n{c.content}\n\n"

    prompt = f"""
【用户问题】
{query}

{context}

【要求】
- 仅基于参考资料回答
- 必须引用编号
- 不允许编造
"""

    return prompt
```

---

# 🛑 4.5 生成模块（generator）

---

## 4.5.1 Prompt模板（关键）

```text
请基于参考资料回答问题：

要求：
1. 仅使用提供内容
2. 每个结论必须标注来源 [编号]
3. 若无法确定，请回答“未找到依据”
4. 禁止推测

回答：
```

---

## 4.5.2 调用

```python
response = llm.generate(prompt)
```

---

# 📌 4.6 引用对齐模块

---

## 4.6.1 输出格式

```text
答案：
xxx [1][2]

来源：
[1] docA - 第3节
[2] docB - 表2
```

---

## 4.6.2 实现

```python
def attach_citations(answer, chunks):
    citations = []
    for i, c in enumerate(chunks):
        citations.append(f"[{i+1}] {c.doc_id}-{c.section}")
    return answer, citations
```

---

# 🧠 4.7 对话记忆模块（升级你现有代码🔥）

---

## 改造点1：约束记忆

```python
self.constraints = [
    "必须提供引用",
    "禁止编造"
]
```

---

## 改造点2：Query Rewrite

```python
def rewrite_query(query, summary):
    prompt = f"""
历史摘要：
{summary}

当前问题：
{query}

请补全上下文，使问题完整
"""
    return llm.generate(prompt)
```

---

# 🛡 4.8 答案校验模块（Verifier）

---

## 4.8.1 校验逻辑

```python
def verify_answer(answer, context):
    prompt = f"""
答案：
{answer}

参考资料：
{context}

判断：
答案是否完全基于资料？
输出：YES / NO
"""
    return llm.generate(prompt)
```

---

## 4.8.2 重试机制

```python
if verify == "NO":
    regenerate()
```

---

# ⚡ 5. 性能优化

---

## 5.1 并行检索

```python
vec_future = executor.submit(vector_search, query)
bm25_future = executor.submit(bm25_search, query)
```

---

## 5.2 缓存

```python
cache[query] = answer
```

---

## 5.3 降低token

* chunk截断
* top_k 控制

---

# 📊 6. 预期效果

| 指标   | 优化后    |
| ---- | ------ |
| 召回率  | ≥90%   |
| 幻觉率  | ≤3%    |
| 延迟   | 2.5~3s |
| 多轮稳定 | ✔      |

---

# 🧪 7. 评估指标设计（论文可用）

```text
1. Recall@K
2. MRR（排序质量）
3. Hallucination Rate
4. Citation Accuracy
5. Response Time
```

---

# 🎯 8. 一句话总结

👉 本系统通过**结构感知切片、混合检索、重排序、受控生成与答案校验机制**，显著提升医疗文档问答系统的准确性与可靠性，实现低幻觉、可溯源的智能问答。

---
