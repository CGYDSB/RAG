# 垂直领域复杂文档 RAG 系统技术方案

**项目**：医疗领域内部技术文档智能问答系统
**技术栈**：Python / FastAPI / ChromaDB / BGE / DeepSeek

---

## 一、系统架构概览

```
用户问题
   ↓
Embedding（bge-small-zh-v1.5）→ 查询向量化
   ↓
混合检索（向量检索 + BM25关键词检索）→ Top-K 候选 chunks
   ↓
相似度阈值过滤（去除噪声）
   ↓
Token 预算控制 → 构造上下文
   ↓
对话历史压缩注入（多轮场景）
   ↓
Prompt 构造（防幻觉 + 引用标注）
   ↓
LLM 生成（DeepSeek）→ 带 [[序号]] 引用的回答
   ↓
返回答案 + 来源文件 + 页码
```

---

## 二、必做部分一：文档解析与分块

### 2.1 文本提取策略

支持 PDF / TXT / MD / DOCX / HTML 五种格式，核心在 PDF 处理上做了专项优化：

**PDF 文本清洗流程**（针对中文 PDF 提取乱码问题）：

```python
def _clean_pdf_text(self, text: str) -> str:
    # 1. 单个换行（断词）→ 空格，保留双换行（段落）
    text = re.sub(r'(?<!\n)\n(?!\n)', ' ', text)
    # 2. 去除多余空格
    text = re.sub(r'[ \t]+', ' ', text)
    # 3. 关键：去除中文字符之间的空格（PDF提取常见问题）
    text = re.sub(r'(?<=[\u4e00-\u9fff])\s+(?=[\u4e00-\u9fff])', '', text)
    # 4. 去除中文和标点之间的空格
    text = re.sub(r'(?<=[\u4e00-\u9fff])\s+(?=[，。！？、；：])', '', text)
    return text.strip()
```

每页文本插入 `[PAGE:N]` 标记，切分后 chunk 可反查原始页码，实现精准溯源。

### 2.2 分块策略

采用**递归字符切分**（RecursiveCharacterSplitter），按语义边界优先级切分：

```
段落（\n\n）→ 行（\n）→ 句子（. ）→ 单词（空格）→ 字符
```

关键参数配置：

| 参数 | 值 | 说明 |
|------|-----|------|
| chunk_size | 256 | 较小粒度，语义更聚焦 |
| chunk_overlap | 30 | 重叠保留上下文连贯性 |

**为什么选 256 而不是 512**：医疗技术文档中每个知识点相对独立，较小的 chunk 能让 embedding 向量更精准地表达单一语义，避免多个不相关知识点混入同一向量。

---

## 三、必做部分二：基础召回架构（混合检索）

### 3.1 整体流程

```
Query
  ├─→ Embedding（bge-small-zh-v1.5）→ 向量检索（ChromaDB）→ Top-10 候选
  └─→ BM25 关键词检索（倒排索引）→ Top-10 候选
         ↓
    加权融合（Reciprocal Score Fusion）
         ↓
    相似度阈值过滤（≥ 0.5）
         ↓
    Top-5 最终结果
```

### 3.2 向量检索

使用 `BAAI/bge-small-zh-v1.5` 作为 Embedding 模型，专为中文语义理解优化。

bge 模型的特殊处理：query 检索时加前缀提升召回精度：

```python
# 检索时加前缀（文档索引时不加）
query_text = "为这个句子生成表示以用于检索相关文章：" + question
```

### 3.3 关键词检索（BM25 简化版）

基于 TF-IDF 思想的倒排索引，补充向量检索对专有名词、型号等精确匹配的不足：

```python
def _keyword_search(self, query: str, top_k: int) -> List[Dict]:
    tokens = self._tokenize(query)
    scores = defaultdict(float)
    for token in tokens:
        # IDF 加权
        idf = np.log((total_docs + 1) / (doc_freq[token] + 1))
        for doc_id, freq in keyword_index[token].items():
            scores[doc_id] += freq * idf
    return sorted(scores, key=scores.get, reverse=True)[:top_k]
```

### 3.4 融合策略

两路结果归一化后加权融合：

```python
final_score = 0.7 × vector_score + 0.3 × keyword_score
```

**权重设计逻辑**：
- 向量检索权重更高（0.7），因为语义理解是主要需求
- 关键词权重（0.3）保证专有名词、型号等精确词不被遗漏
- 两路都命中的 chunk 得分叠加，天然实现"语义+精确"双重过滤

---

## 四、必做部分三：Prompt 工程与防幻觉控制

### 4.1 主问答 Prompt 设计

针对医疗领域高精度要求，设计了严格的防幻觉 Prompt：

```
你是一个专业的技术文档问答助手，服务于医疗领域内部知识库。

【回答规则】
1. 只能基于下方"参考文档"中的内容回答，禁止凭空捏造任何信息
2. 每一个关键事实必须用 [[序号]] 标注来源
3. 如果参考文档中没有足够信息，必须明确回复："根据现有文档，无法回答该问题"
4. 涉及剂量、操作规程、安全警告等医疗敏感内容，必须原文引用，不得改写
5. 用中文回答，专业术语保持原文

【参考文档】
{context}

【用户问题】
{question}
```

**防幻觉的三层机制**：

1. Prompt 层：明确禁止捏造，无法回答时必须说"无法回答"
2. 检索层：相似度阈值过滤（≥0.5），低相关 chunk 不送入 LLM
3. 引用层：每个事实强制标注 `[[序号]]`，用户可追溯原文

### 4.2 引用溯源实现

每个 chunk 在索引时记录页码 metadata，回答时输出来源信息：

```
[1] Score: 0.700
    Source: python-doc-27-34.pdf
    Page:   10
```

用户看到 `[[1]]` 引用，可直接翻到 PDF 第 10 页核对原文。

### 4.3 Prompt 模板体系

系统维护多套 Prompt 模板，按场景切换：

| 模板 | 场景 |
|------|------|
| `system.rag_assistant` | 默认问答，严格防幻觉 |
| `system.conversational` | 多轮对话，携带历史摘要 |
| `system.concise` | 快速返回，简短回答 |
| `system.detailed` | 报告场景，结构化输出 |

---

## 五、必做部分四：长上下文优化策略

### 5.1 问题背景

8K token 窗口需要容纳：系统提示 + 对话历史 + 检索文档 + 用户问题 + 回答空间，总量可能超过 20K。

### 5.2 分层 Token 预算分配

```
总预算：7000 tokens（8K窗口留1K余量）
├── 系统提示：500 tokens（固定，永远保留）
├── 对话历史：1000 tokens（压缩后）
├── 检索文档：4000 tokens（按相关性截断）
├── 用户问题：200 tokens
└── 回答空间：1500 tokens（预留给 LLM 输出）
```

### 5.3 对话历史压缩策略

```
对话历史（N轮）
    ├── 最近 2 轮：完整保留（~1000 tokens）
    └── 更早的轮次：LLM 压缩成摘要（~200 tokens）
              ↓
         压缩 Prompt：
         "保留：核心问题、关键事实、未解决疑问
          丢弃：重复信息、寒暄、已解决细节"
```

核心代码：

```python
class ConversationManager:
    def compress_history(self, generator, gen_config) -> str:
        older_turns = self.get_older_turns()  # 超出keep_recent的历史
        history_text = "\n".join([
            f"用户：{t.question}\n助手：{t.answer[:500]}"
            for t in older_turns
        ])
        # 用 LLM 压缩，失败时 fallback 截断
        result = generator.generate(compress_prompt, gen_config)
        self.summary = result.text.strip()
```

### 5.4 检索文档截断策略

```python
def _build_context_with_budget(self, chunks) -> str:
    budget_chars = 4000 * 3  # token预算转字符数
    for i, chunk in enumerate(chunks):
        # 单个 chunk 超过 800 字符时截断
        if len(chunk.text) > 800:
            text = chunk.text[:800] + "..."
        # 总预算不足时停止添加
        if total + len(formatted) > budget_chars:
            break
```

**信息保留优先级**（从高到低）：
1. 系统提示（角色定义 + 约束规则）
2. 最近 2 轮完整对话（保持对话连贯性）
3. 早期历史摘要（保留关键约束和实体）
4. 当前轮检索文档（按相关性分数排序截断）
5. 用户当前问题

---

## 六、系统技术选型说明

| 组件 | 选型 | 理由 |
|------|------|------|
| Embedding | BAAI/bge-small-zh-v1.5 | 专为中文优化，本地运行无 API 费用 |
| 向量数据库 | ChromaDB | 本地持久化，无需额外服务，开发友好 |
| LLM | DeepSeek-chat | 兼容 OpenAI 接口，成本低，中文能力强 |
| 检索框架 | 自研 HybridRetriever | 向量 + BM25 双路，可插拔架构 |
| 服务框架 | FastAPI | 自动生成 Swagger 文档，支持流式输出 |

---

## 七、待完善部分

> 以下内容后续补充

- [ ] Reranker 重排序效果对比实验
- [ ] 评估指标基线数据（Faithfulness / Relevancy / Precision）
- [ ] 选做方向（表格解析 / 模型微调 / 自动化评估）
