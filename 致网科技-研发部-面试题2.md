## 垂直领域复杂文档的 RAG 系统设计

### 1. 业务背景

我们要为一家**医疗领域公司**建立内部技术文档智能问答系统。原始文档格式复杂（含表格、专有名词），且对回答的准确性要求极高（严控幻觉，必须提供来源引用）。现有系统采用固定长度切片+简单向量检索+LLM直接生成，当前指标：召回率65%，幻觉率15%，延迟4.2s，20轮对话后模型遗忘关键约束，新系统设计时需考虑上述问题。

**任务要求：**

请准备一份技术方案（PPT或文档），根据实际情况、可准备相关的 Python 核心逻辑伪代码或 Demo。方案需包含以下“必做”部分，并可根据自身技术背景选择完成最多一个“选做”部分。

**方案阐述限时10分钟。**

------

### 2. 必做部分

1. **文档解析与分块（Parsing & Chunking）**
   - 面对包含大量行业术语的技术手册，你会采用什么策略进行文本提取和切片？
   - 如何尽可能保留文档的上下文语义连贯性？
2. **基础召回架构（Retrieval）**
   - 设计一个混合检索（Hybrid Search）流程（如：向量检索 + 关键词检索）。
   - 请简述你如何整合这两路召回的结果，并给出逻辑说明。
3. **Prompt 工程与防幻觉控制**
   - 如何构建 Prompt 将召回的片段喂给大模型？
   - 如果召回的片段无法回答用户的问题，如何通过系统设计或 Prompt 限制模型“胡编乱造”？
   - 如何实现精准的“引用溯源（Citations）”，让用户知道答案来自哪份文档的哪一页？
4. **长上下文优化策略（Long-context Management）**
   - 当用户进行多轮技术咨询，系统需携带前5轮对话历史+本次召回的5篇长文档（每篇5000+字），总Token可能超过20K。
   - 请设计一种**上下文压缩或分层加载**机制，在8K上下文窗口限制下保证回答质量，并说明哪些信息应保留、哪些可摘要或丢弃。

------

### 3. 选做部分

- **选做方向 A：复杂结构化数据解析（多模态/表格降维）**
  - **痛点**：PDF 中存在大量跨页的嵌套表格或带有文字说明的零部件图纸，常规 Chunking 会导致表格结构碎裂。
  - **挑战**：请设计一种方案专门处理这类复杂表格/图文。
- **选做方向 B：垂直领域语义偏移与模型微调**
  - **痛点**：开源的 Embedding 模型（如 BGE 等）在通用语料上表现很好，但遇到贵公司的内部专有名词、缩写（如“XYZ-990a 模块”）时，向量表征极其糟糕。
  - **挑战**：除了业务同义词表外，如果在资源允许的情况下，你会如何利用业务数据对 Embedding 模型或 ReRank模型进行**微调（Fine-tuning）**？请简述训练数据的构造方式和损失函数选择。
- **选做方向 C：系统全链路自动化评估（Eval）**
  - **痛点**：系统上线后，不能只靠人工抽查来判断“回答得好不好”。
  - **挑战**：请参考业界前沿技术，构建一套自动化的评估指标与流水线？

