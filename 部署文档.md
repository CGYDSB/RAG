# Production RAG System 本地部署文档

## 环境要求

- Miniconda 或 Anaconda（推荐 Miniconda：https://docs.conda.io/en/latest/miniconda.html）
- Git（可选）
- （Docker 部署方式需要）Docker Desktop

---

## 方式一：conda 本地运行（推荐）

### 第一步：创建 conda 虚拟环境

```bash
conda create -n rag-system python=3.11 -y
```

激活环境：

```bash
conda activate rag-system
```

激活成功后，命令行前缀会出现 `(rag-system)`。

> 后续每次重新打开终端，都需要先执行 `conda activate rag-system` 再操作项目。

---

### 第二步：安装 PyTorch

没有 NVIDIA GPU（CPU 模式）：

```bash
conda install pytorch cpuonly -c pytorch -y
```

有 NVIDIA GPU（根据你的 CUDA 版本选择）：

```bash
# CUDA 11.8
conda install pytorch pytorch-cuda=11.8 -c pytorch -c nvidia -y
# CUDA 12.1
conda install pytorch pytorch-cuda=12.1 -c pytorch -c nvidia -y
```

---

### 第三步：安装其余依赖

```bash
pip install -r requirements.txt
```

> 如果网络较慢，可以使用国内镜像加速：
> ```bash
> pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
> ```

---

### 第三步：配置环境变量

```bash
cp .env.example .env
```

用文本编辑器打开 `.env`，填入你的 OpenAI API Key：

```
OPENAI_API_KEY=sk-你的真实key
```

其余配置保持默认即可。

---

### 第四步：创建必要目录

```bash
mkdir -p data/raw data/processed logs
```

---

### 第五步：修改配置（可选，CPU 用户必做）

打开 `config/settings.yaml`，找到以下内容：

```yaml
embedding:
  device: "cuda"
```

如果你没有 NVIDIA GPU，改为：

```yaml
embedding:
  device: "cpu"
```

如果不想使用 OpenAI Embedding（节省费用），可以改为本地模型：

```yaml
embedding:
  provider: "sentence-transformers"
  model: "all-MiniLM-L6-v2"
  device: "cpu"
```

---

### 第六步：启动 API 服务

```bash
python main.py server --host 0.0.0.0 --port 8000 --reload
```

启动成功后，终端会显示：

```
INFO: Uvicorn running on http://0.0.0.0:8000
```

浏览器访问 `http://localhost:8000/docs` 可以看到 Swagger 接口文档。

---

### 第七步：导入文档（可选）

把你的文档（PDF / TXT / MD / DOCX / HTML）放入 `data/raw/` 目录，然后执行：

```bash
python main.py index --source data/raw/
```

---

### 第八步：命令行问答测试

```bash
python main.py query "你的问题"
```

流式输出模式：

```bash
python main.py query "你的问题" --stream
```

---

## 方式二：Docker Compose 一键部署（推荐生产）

### 前置要求

安装并启动 Docker Desktop：https://www.docker.com/products/docker-desktop/

### 第一步：配置环境变量

```bash
cp .env.example .env
# 编辑 .env，填入 OPENAI_API_KEY
```

### 第二步：一键启动所有服务

```bash
docker-compose up -d
```

这会自动启动：
- `rag_api`：FastAPI 服务（端口 8000）
- `rag_qdrant`：Qdrant 向量数据库（端口 6333）
- `rag_redis`：Redis 缓存（端口 6379）
- `rag_chromadb`：ChromaDB（端口 8001）

### 第三步：查看启动日志

```bash
docker-compose logs -f api
```

### 第四步：验证服务

```bash
curl http://localhost:8000/health
```

返回 `{"status":"healthy",...}` 表示启动成功。

### 停止服务

```bash
docker-compose down
```

---

## 接口使用说明

服务启动后，访问 `http://localhost:8000/docs` 查看完整接口文档。

### 问答接口

```bash
curl -X POST http://localhost:8000/query \
  -H "Content-Type: application/json" \
  -d '{"question": "什么是RAG？", "top_k": 5}'
```

### 上传文档

```bash
curl -X POST http://localhost:8000/upload \
  -F "file=@your_document.pdf"
```

### 健康检查

```bash
curl http://localhost:8000/health
```

---

## 常见问题

**Q：安装 torch 失败或很慢？**

用 conda 单独安装 CPU 版本：
```bash
conda install pytorch cpuonly -c pytorch -y
```

**Q：提示 `OPENAI_API_KEY not provided`？**

检查 `.env` 文件是否存在，且 key 填写正确，没有多余空格。

**Q：ChromaDB 报错 `Settings` 找不到？**

chromadb 新版本 API 有变化，本项目已使用 `PersistentClient`，确保版本为 `chromadb==0.4.18`：
```bash
pip install chromadb==0.4.18
```

**Q：想用免费的本地 Embedding 模型，不消耗 OpenAI 费用？**

修改 `config/settings.yaml`：
```yaml
embedding:
  provider: "sentence-transformers"
  model: "all-MiniLM-L6-v2"
  device: "cpu"
```
首次运行会自动下载模型（约 90MB）。
